#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);


    int h;
    cin >> h;

    if(h == 1) cout << 2;
    else cout << 1;



    return 0;
}